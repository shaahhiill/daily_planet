// Central config for the Daily Planet app.
// Change the backendUrl here once — all services pick it up automatically.
class AppConfig {
  // The deployed backend URL. Replace this with your Render/Railway URL after deploying.
  // Example: 'https://daily-planet-api.onrender.com'
  static const String backendUrl = 'https://daily-planet-api.onrender.com';
}
